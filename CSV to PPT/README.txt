Before using you have to install the python-pptx module
Use:
pip install python-pptx

Then just run the convert.py file
The ppt will come in test.pptx file
empty2.pptx is the base ppt. Don't change that
It reads from IPL.csv Make sure the format is the same for corresponding CSVs